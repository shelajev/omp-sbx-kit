# The TCK prompt check invokes omp with these arguments. It still requires an
# OpenRouter credential binding because the kit deliberately marks it required.
promptArgs: ["--model", "openrouter/openai/gpt-5.5", "--auto-approve", "--print"]
